<h1>Products</h1>

<% @products.each do |product| %>
  <div class="col-xs-6 col-md-3">
    <h3><%= product.name %></h3>
    <%= link_to product_path(permalink: product.permalink) do %>
      <%= image_tag product.photo, class: "img-responsive img-thumbnail" %>
    <% end %>
  </div>
<% end %>
